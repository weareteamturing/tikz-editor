{
  "\\path": {
    "signature": "\\path⟨specification⟩;",
    "default": "",
    "description": "This command is available only inside a {tikzpicture} environment.\n\nThe ⟨specification⟩ is a long stream of path operations. Most of these path operations tell TikZ how the path is built. For example, when you write --(0,0), you use a line-to operation and it means “continue the path from wherever you are to the origin”.",
    "type": "command"
  },
  "/tikz/every path": {
    "signature": "/tikz/every path",
    "default": "style, initially empty",
    "description": "This style is installed at the beginning of every path. This can be useful for (temporarily) adding, say, the draw option to everything in a scope.",
    "type": "style"
  },
  "every path": {
    "signature": "/tikz/every path",
    "default": "style, initially empty",
    "description": "This style is installed at the beginning of every path. This can be useful for (temporarily) adding, say, the draw option to everything in a scope.",
    "type": "style"
  },
  "/tikz/insert path": {
    "signature": "/tikz/insert path=⟨path⟩",
    "default": "no default",
    "description": "This key can be used inside an option to add something to the current path. This is mostly useful for defining styles that create graphic contents. This option should be used with care, for instance it should not be used as an argument of, say, a node. In the following example, we use a style to add little circles to a path.",
    "type": "key"
  },
  "insert path": {
    "signature": "/tikz/insert path=⟨path⟩",
    "default": "no default",
    "description": "This key can be used inside an option to add something to the current path. This is mostly useful for defining styles that create graphic contents. This option should be used with care, for instance it should not be used as an argument of, say, a node. In the following example, we use a style to add little circles to a path.",
    "type": "key"
  },
  "/tikz/append after command": {
    "signature": "/tikz/append after command=⟨path⟩",
    "default": "no default",
    "description": "Some of the path commands described in the following sections take optional arguments. For these commands, when you use this key inside these options, the ⟨path⟩ will be inserted after the path command is done. For instance, when you give this command in the option list of a node, the ⟨path⟩ will be added after the node. This is used by, for instance, the label option to allow you to specify a label in the option list of a node, but have this label cause a node to be added after another node.",
    "type": "key"
  },
  "append after command": {
    "signature": "/tikz/append after command=⟨path⟩",
    "default": "no default",
    "description": "Some of the path commands described in the following sections take optional arguments. For these commands, when you use this key inside these options, the ⟨path⟩ will be inserted after the path command is done. For instance, when you give this command in the option list of a node, the ⟨path⟩ will be added after the node. This is used by, for instance, the label option to allow you to specify a label in the option list of a node, but have this label cause a node to be added after another node.",
    "type": "key"
  },
  "/tikz/prefix after command": {
    "signature": "/tikz/prefix after command=⟨path⟩",
    "default": "no default",
    "description": "Works like append after command, only the accumulation order is inverse: The ⟨path⟩ is added before any earlier paths added using either append after command or prefix after command.\n\nThe perhaps simplest operation is the move-to operation, which is specified by just giving a coordinate where a path operation is expected.",
    "type": "key"
  },
  "prefix after command": {
    "signature": "/tikz/prefix after command=⟨path⟩",
    "default": "no default",
    "description": "Works like append after command, only the accumulation order is inverse: The ⟨path⟩ is added before any earlier paths added using either append after command or prefix after command.\n\nThe perhaps simplest operation is the move-to operation, which is specified by just giving a coordinate where a path operation is expected.",
    "type": "key"
  },
  "--": {
    "signature": "\\path … --⟨coordinate or cycle⟩ …;",
    "default": "",
    "description": "The line-to operation extends the current path from the current point in a straight line to the given ⟨coordinate⟩ (the “or cycle” part is explained in a moment). The “current point” is the endpoint of the previous drawing operation or the point specified by a prior move-to operation.\n\nWhen a line-to operation is used and some path segment has just been constructed, for example by another line-to operation, the two line segments become joined. This means that if they are drawn, the point where they meet is “joined” smoothly. To appreciate the difference, consider the following two examples: In the left example, the path consists of two path segments that are not joined, but they happen to share a point, while in the right example a smooth join is shown.",
    "type": "key"
  },
  "-bar/": {
    "signature": "",
    "default": "",
    "description": "\\begin{ tikzpicture} \\draw (0,0) node(a) [ draw] {A} (1,1) node(b) [ draw] {B}; \\draw (a.north) |- (b.west); \\draw[ color=red] (a.east) -| (2,1.5) -| (b.north); \\end{ tikzpicture}\n\nInstead of a coordinate you can also write cycle to close the path:",
    "type": "key"
  },
  "bar/-": {
    "signature": "",
    "default": "",
    "description": "The curve-to operation allows you to extend a path using a Bézier curve.",
    "type": "key"
  },
  "..": {
    "signature": "\\path … ..controls⟨c⟩and⟨d⟩..⟨y or cycle⟩ …;",
    "default": "",
    "description": "This operation extends the current path from the current point, let us call it \\(x\\), via a curve to a point \\(y\\) (if, instead of a coordinate you say cycle at the end, \\(y\\) will be the coordinate of the last move-to operation). The curve is a cubic Bézier curve. For such a curve, apart from \\(y\\), you also specify two control points \\(c\\) and \\(d\\). The idea is that the curve starts at \\(x\\), “heading” in the direction of \\(c\\). Mathematically spoken, the tangent of the curve at \\(x\\) goes through \\(c\\). Similarly, the curve ends at \\(y\\), “coming from” the other control point, \\(d\\). The larger the distance between \\(x\\) and \\(c\\) and between \\(d\\) and \\(y\\), the larger the curve will be.\n\nIf the “and⟨d⟩” part is not given, \\(d\\) is assumed to be equal to \\(c\\).",
    "type": "key"
  },
  "rectangle": {
    "signature": "\\path … rectangle⟨corner or cycle⟩ …;",
    "default": "",
    "description": "When this operation is used, one corner will be the current point, another corner is given by ⟨corner⟩, which becomes the new current point.",
    "type": "key"
  },
  "/tikz/rounded corners": {
    "signature": "/tikz/rounded corners=⟨inset⟩",
    "default": "default 4pt",
    "description": "When this option is in force, all corners (places where a line is continued either via line-to or a curve-to operation) are replaced by little arcs so that the corner becomes smooth.",
    "type": "key"
  },
  "rounded corners": {
    "signature": "/tikz/rounded corners=⟨inset⟩",
    "default": "default 4pt",
    "description": "When this option is in force, all corners (places where a line is continued either via line-to or a curve-to operation) are replaced by little arcs so that the corner becomes smooth.",
    "type": "key"
  },
  "/tikz/sharp corners": {
    "signature": "/tikz/sharp corners",
    "default": "no value",
    "description": "This options switches off any rounding on subsequent corners of the path.\n\nCircles and ellipses are common path elements for which there is a special path operation.",
    "type": "key"
  },
  "sharp corners": {
    "signature": "/tikz/sharp corners",
    "default": "no value",
    "description": "This options switches off any rounding on subsequent corners of the path.\n\nCircles and ellipses are common path elements for which there is a special path operation.",
    "type": "key"
  },
  "circle": {
    "signature": "\\path … circle[⟨options⟩] …;",
    "default": "",
    "description": "This command adds a circle to the current path where the center of the circle is the current point by default, but you can use the at option to change this. The new current point of the path will be (typically just remain) the center of the circle.\n\nThe radius of the circle is specified using the following options:",
    "type": "key"
  },
  "/tikz/x radius": {
    "signature": "/tikz/x radius=⟨value⟩",
    "default": "no default",
    "description": "Sets the horizontal radius of the circle (which, when this value is different from the vertical radius, is actually an ellipse). The ⟨value⟩ may either be a dimension or a dimensionless number. In the latter case, the number is interpreted in the \\(xy\\)-coordinate system (if the \\(x\\)-unit is set to, say, 2cm, then x radius=3 will have the same effect as x radius=6cm).",
    "type": "key"
  },
  "/tikz/y radius": {
    "signature": "/tikz/y radius=⟨value⟩",
    "default": "no default",
    "description": "Works like the x radius.",
    "type": "key"
  },
  "/tikz/radius": {
    "signature": "/tikz/radius=⟨value⟩",
    "default": "no default",
    "description": "Sets the x radius and y radius simultaneously.",
    "type": "key"
  },
  "/tikz/at": {
    "signature": "/tikz/at=⟨coordinate⟩",
    "default": "no default",
    "description": "If this option is explicitly set inside the ⟨options⟩ (or indirectly via the every circle style), the ⟨coordinate⟩ is used as the center of the circle instead of the current point. Setting at to some value in an enclosing scope has no effect.\n\nThe ⟨options⟩ may also contain additional options like, say, a rotate or scale, that will only have an effect on the circle.",
    "type": "key"
  },
  "at": {
    "signature": "/tikz/at=⟨coordinate⟩",
    "default": "no default",
    "description": "If this option is explicitly set inside the ⟨options⟩ (or indirectly via the every circle style), the ⟨coordinate⟩ is used as the center of the circle instead of the current point. Setting at to some value in an enclosing scope has no effect.\n\nThe ⟨options⟩ may also contain additional options like, say, a rotate or scale, that will only have an effect on the circle.",
    "type": "key"
  },
  "/tikz/every circle": {
    "signature": "/tikz/every circle",
    "default": "style, no value",
    "description": "You can use this key to set up, say, a default radius for every circle. The key will also be used with the ellipse operation.\n\nIn case you feel that the names radius and x radius are too long for your taste, you can easily created shorter aliases:",
    "type": "style"
  },
  "every circle": {
    "signature": "/tikz/every circle",
    "default": "style, no value",
    "description": "You can use this key to set up, say, a default radius for every circle. The key will also be used with the ellipse operation.\n\nIn case you feel that the names radius and x radius are too long for your taste, you can easily created shorter aliases:",
    "type": "style"
  },
  "ellipse": {
    "signature": "\\path … ellipse[⟨options⟩] …;",
    "default": "",
    "description": "This command has exactly the same effect as circle. The older syntax for this command is ellipse (⟨x radius⟩ and ⟨y radius⟩). As for the circle command, this syntax is not as good as the standard syntax.",
    "type": "key"
  },
  "arc": {
    "signature": "\\path … arc[⟨options⟩] …;",
    "default": "",
    "description": "The arc operation adds a part of an ellipse to the current path. The radii of the ellipse are given by the values of x radius and y radius, which should be set in the ⟨options⟩. The arc will start at the current point and will end at the end of the arc. The arc will start and end at angles computed from the three keys start angle, end angle, and delta angle. Normally, the first two keys specify the start and end angle. However, in case one of them is empty, it is computed from the other key plus or minus the delta angle. In detail, if end angle is empty, it is set to the start angle plus the delta angle. If the start angle is missing, it is set to the end angle minus the delta angle. If all three keys are set, the delta angle is ignored.",
    "type": "key"
  },
  "/tikz/start angle": {
    "signature": "/tikz/start angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the start angle.",
    "type": "key"
  },
  "start angle": {
    "signature": "/tikz/start angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the start angle.",
    "type": "key"
  },
  "/tikz/end angle": {
    "signature": "/tikz/end angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the end angle.",
    "type": "key"
  },
  "end angle": {
    "signature": "/tikz/end angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the end angle.",
    "type": "key"
  },
  "/tikz/delta angle": {
    "signature": "/tikz/delta angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the delta angle.",
    "type": "key"
  },
  "delta angle": {
    "signature": "/tikz/delta angle=⟨degrees⟩",
    "default": "no default",
    "description": "Sets the delta angle.",
    "type": "key"
  },
  "grid": {
    "signature": "\\path … grid[⟨options⟩]⟨corner or cycle⟩ …;",
    "default": "",
    "description": "This operation adds a grid filling a rectangle whose two corners are given by ⟨corner⟩ and by the previous coordinate. (Instead of a coordinate you can also say cycle to use the position of the last move-to as the corner coordinate, but it is not very natural to do so.) Thus, the typical way in which a grid is drawn is \\draw (1,1) grid (3,3);, which yields a grid filling the rectangle whose corners are at \\((1,1)\\) and \\((3,3)\\). All coordinate transformations apply to the grid.",
    "type": "key"
  },
  "/tikz/step": {
    "signature": "/tikz/step=⟨number or dimension or coordinate⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in both the \\(x\\) and \\(y\\)-direction. If a dimension is provided, this is used directly. If a number is provided, this number is interpreted in the \\(xy\\)-coordinate system. For example, if you provide the number 2, then the \\(x\\)-step is twice the \\(x\\)-vector and the \\(y\\)-step is twice the \\(y\\)-vector set by the x= and y= options. Finally, if you provide a coordinate, then the \\(x\\)-part of this coordinate will be used as the \\(x\\)-step and the \\(y\\)-part will be used as the \\(y\\)-coordinate.",
    "type": "key"
  },
  "step": {
    "signature": "/tikz/step=⟨number or dimension or coordinate⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in both the \\(x\\) and \\(y\\)-direction. If a dimension is provided, this is used directly. If a number is provided, this number is interpreted in the \\(xy\\)-coordinate system. For example, if you provide the number 2, then the \\(x\\)-step is twice the \\(x\\)-vector and the \\(y\\)-step is twice the \\(y\\)-vector set by the x= and y= options. Finally, if you provide a coordinate, then the \\(x\\)-part of this coordinate will be used as the \\(x\\)-step and the \\(y\\)-part will be used as the \\(y\\)-coordinate.",
    "type": "key"
  },
  "/tikz/xstep": {
    "signature": "/tikz/xstep=⟨dimension or number⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in the \\(x\\)-direction.",
    "type": "key"
  },
  "xstep": {
    "signature": "/tikz/xstep=⟨dimension or number⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in the \\(x\\)-direction.",
    "type": "key"
  },
  "/tikz/ystep": {
    "signature": "/tikz/ystep=⟨dimension or number⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in the \\(y\\)-direction.\n\nIt is important to note that the grid is always “phased” such that it contains the point \\((0,0)\\) if that point happens to be inside the rectangle. Thus, the grid does not always have an intersection at the corner points; this occurs only if the corner points are multiples of the stepping. Note that due to rounding errors, the “last” lines of a grid may be omitted. In this case, you have to add an epsilon to the corner points.",
    "type": "key"
  },
  "ystep": {
    "signature": "/tikz/ystep=⟨dimension or number⟩",
    "default": "no default, initially 1cm",
    "description": "Sets the stepping in the \\(y\\)-direction.\n\nIt is important to note that the grid is always “phased” such that it contains the point \\((0,0)\\) if that point happens to be inside the rectangle. Thus, the grid does not always have an intersection at the corner points; this occurs only if the corner points are multiples of the stepping. Note that due to rounding errors, the “last” lines of a grid may be omitted. In this case, you have to add an epsilon to the corner points.",
    "type": "key"
  },
  "/tikz/help lines": {
    "signature": "/tikz/help lines",
    "default": "style, initially line width=0.2pt,gray",
    "description": "This style makes lines “subdued” by using thin gray lines for them. However, this style is not installed automatically and you have to say for example:",
    "type": "style"
  },
  "help lines": {
    "signature": "/tikz/help lines",
    "default": "style, initially line width=0.2pt,gray",
    "description": "This style makes lines “subdued” by using thin gray lines for them. However, this style is not installed automatically and you have to say for example:",
    "type": "style"
  },
  "parabola": {
    "signature": "\\path … parabola[⟨options⟩]bend⟨bend coordinate⟩⟨coordinate or cycle⟩ …;",
    "default": "",
    "description": "This operation adds a parabola through the current point and the given ⟨coordinate⟩ or, if cycle is used instead of coordinate at the end, the ⟨coordinate⟩ is set to the position of the last move-to and the path gets closed after the parabola. If the bend is given, it specifies where the bend should go; the ⟨options⟩ can also be used to specify where the bend is. By default, the bend is at the old current point.",
    "type": "key"
  },
  "/tikz/bend": {
    "signature": "/tikz/bend=⟨coordinate⟩",
    "default": "no default",
    "description": "Has the same effect as saying bend⟨coordinate⟩ outside the ⟨options⟩. The option specifies that the bend of the parabola should be at the given ⟨coordinate⟩. You have to take care yourself that the bend position is a “valid” position; which means that if there is no parabola of the form \\(f(x) = a x^2 + b x + c\\) that goes through the old current point, the given bend, and the new current point, the result will not be a parabola.\n\nThere is one special property of the ⟨coordinate⟩: When a relative coordinate is given like +(0,0), the position relative to this coordinate is “flexible”. More precisely, this position lies somewhere on a line from the old current point to the new current point. The exact position depends on the next option.",
    "type": "key"
  },
  "bend": {
    "signature": "/tikz/bend=⟨coordinate⟩",
    "default": "no default",
    "description": "Has the same effect as saying bend⟨coordinate⟩ outside the ⟨options⟩. The option specifies that the bend of the parabola should be at the given ⟨coordinate⟩. You have to take care yourself that the bend position is a “valid” position; which means that if there is no parabola of the form \\(f(x) = a x^2 + b x + c\\) that goes through the old current point, the given bend, and the new current point, the result will not be a parabola.\n\nThere is one special property of the ⟨coordinate⟩: When a relative coordinate is given like +(0,0), the position relative to this coordinate is “flexible”. More precisely, this position lies somewhere on a line from the old current point to the new current point. The exact position depends on the next option.",
    "type": "key"
  },
  "/tikz/bend pos": {
    "signature": "/tikz/bend pos=⟨fraction⟩",
    "default": "no default",
    "description": "Specifies where the “previous” point is relative to which the bend is calculated. The previous point will be at the ⟨fraction⟩th part of the line from the old current point to the new current point.\n\nThe idea is the following: If you say bend pos=0 and bend +(0,0), the bend will be at the old current point. If you say bend pos=1 and bend +(0,0), the bend will be at the new current point. If you say bend pos=0.5 and bend +(0,2cm) the bend will be 2cm above the middle of the line between the start and end point. This is most useful in situations such as the following:",
    "type": "key"
  },
  "bend pos": {
    "signature": "/tikz/bend pos=⟨fraction⟩",
    "default": "no default",
    "description": "Specifies where the “previous” point is relative to which the bend is calculated. The previous point will be at the ⟨fraction⟩th part of the line from the old current point to the new current point.\n\nThe idea is the following: If you say bend pos=0 and bend +(0,0), the bend will be at the old current point. If you say bend pos=1 and bend +(0,0), the bend will be at the new current point. If you say bend pos=0.5 and bend +(0,2cm) the bend will be 2cm above the middle of the line between the start and end point. This is most useful in situations such as the following:",
    "type": "key"
  },
  "/tikz/parabola height": {
    "signature": "/tikz/parabola height=⟨dimension⟩",
    "default": "no default",
    "description": "This option has the same effect as [bend pos=0.5,bend={+(0pt,⟨dimension⟩)}].",
    "type": "key"
  },
  "parabola height": {
    "signature": "/tikz/parabola height=⟨dimension⟩",
    "default": "no default",
    "description": "This option has the same effect as [bend pos=0.5,bend={+(0pt,⟨dimension⟩)}].",
    "type": "key"
  },
  "/tikz/bend at start": {
    "signature": "/tikz/bend at start",
    "default": "style, no value",
    "description": "This places the bend at the start of a parabola. It is a shortcut for the following options: bend pos=0,bend={+(0,0)}.",
    "type": "style"
  },
  "bend at start": {
    "signature": "/tikz/bend at start",
    "default": "style, no value",
    "description": "This places the bend at the start of a parabola. It is a shortcut for the following options: bend pos=0,bend={+(0,0)}.",
    "type": "style"
  },
  "/tikz/bend at end": {
    "signature": "/tikz/bend at end",
    "default": "style, no value",
    "description": "This places the bend at the end of a parabola.\n\nThe sin and cos operations are similar to the parabola operation. They, too, can be used to draw (parts of) a sine or cosine curve.",
    "type": "style"
  },
  "bend at end": {
    "signature": "/tikz/bend at end",
    "default": "style, no value",
    "description": "This places the bend at the end of a parabola.\n\nThe sin and cos operations are similar to the parabola operation. They, too, can be used to draw (parts of) a sine or cosine curve.",
    "type": "style"
  },
  "sin": {
    "signature": "\\path … sin⟨coordinate or cycle⟩ …;",
    "default": "",
    "description": "The effect of sin is to draw a scaled and shifted version of a sine curve in the interval \\([0,\\pi /2]\\). The scaling and shifting is done in such a way that the start of the sine curve in the interval is at the old current point and that the end of the curve in the interval is at ⟨coordinate⟩. Here is an example that should clarify this:",
    "type": "key"
  },
  "cos": {
    "signature": "\\path … cos⟨coordinate or cycle⟩ …;",
    "default": "",
    "description": "This operation works similarly, only a cosine in the interval \\([0,\\pi /2]\\) is drawn. By correctly alternating sin and cos operations, you can create a complete sine or cosine curve:",
    "type": "key"
  },
  "svg": {
    "signature": "\\path … svg[⟨options⟩]{⟨path data⟩} …;",
    "default": "",
    "description": "This operation adds the path specified in the ⟨path data⟩ in svg 1.1 path data syntax to the current path. Unlike the svg-specification, it is permissible that the path data does not start with a move-to command (m or M), in which case the last point of the current path is used as start point. The optional ⟨options⟩ apply locally to this path operation, typically you will use them to set up, say, some transformations.",
    "type": "key"
  },
  "to": {
    "signature": "\\path … to[⟨options⟩] ⟨nodes⟩ ⟨coordinate or cycle⟩ …;",
    "default": "",
    "description": "This path operation inserts the path currently set via the to path option at the current position. The ⟨options⟩ can be used to modify (perhaps implicitly) the to path and to set up how the path will be rendered.\n\nBefore the to path is inserted, a number of macros are set up that can “help” the to path. These are \\tikztostart, \\tikztotarget, and \\tikztonodes; they are explained in the following.",
    "type": "key"
  },
  "/tikz/edge node": {
    "signature": "/tikz/edge node=⟨node specification⟩",
    "default": "no default",
    "description": "This key can be used inside the ⟨options⟩ of a to path command. It will add the ⟨node specification⟩ to the list of nodes to be placed on the connecting line, just as if you had written the ⟨node specification⟩ directly after the to keyword:",
    "type": "key"
  },
  "edge node": {
    "signature": "/tikz/edge node=⟨node specification⟩",
    "default": "no default",
    "description": "This key can be used inside the ⟨options⟩ of a to path command. It will add the ⟨node specification⟩ to the list of nodes to be placed on the connecting line, just as if you had written the ⟨node specification⟩ directly after the to keyword:",
    "type": "key"
  },
  "/tikz/edge label": {
    "signature": "/tikz/edge label=⟨text⟩",
    "default": "no default",
    "description": "A shorthand for edge node={node[auto]{⟨text⟩}}.",
    "type": "key"
  },
  "edge label": {
    "signature": "/tikz/edge label=⟨text⟩",
    "default": "no default",
    "description": "A shorthand for edge node={node[auto]{⟨text⟩}}.",
    "type": "key"
  },
  "/tikz/edge label'": {
    "signature": "/tikz/edge label'=⟨text⟩",
    "default": "no default",
    "description": "A shorthand for edge node={node[auto,swap]{⟨text⟩}}.",
    "type": "key"
  },
  "edge label'": {
    "signature": "/tikz/edge label'=⟨text⟩",
    "default": "no default",
    "description": "A shorthand for edge node={node[auto,swap]{⟨text⟩}}.",
    "type": "key"
  },
  "/tikz/every to": {
    "signature": "/tikz/every to",
    "default": "style, initially empty",
    "description": "This style is installed at the beginning of every to.",
    "type": "style"
  },
  "every to": {
    "signature": "/tikz/every to",
    "default": "style, initially empty",
    "description": "This style is installed at the beginning of every to.",
    "type": "style"
  },
  "/tikz/to path": {
    "signature": "/tikz/to path=⟨path⟩",
    "default": "no default",
    "description": "Whenever a to operation is used, the ⟨path⟩ is inserted. More precisely, the following path is added:\n\n{[every to,⟨options⟩] ⟨path⟩ }",
    "type": "key"
  },
  "to path": {
    "signature": "/tikz/to path=⟨path⟩",
    "default": "no default",
    "description": "Whenever a to operation is used, the ⟨path⟩ is inserted. More precisely, the following path is added:\n\n{[every to,⟨options⟩] ⟨path⟩ }",
    "type": "key"
  },
  "\\tikztostart": {
    "signature": "",
    "default": "",
    "description": "\\begin{ tikzpicture}[ to path={ .. controls +(1,0) and +(1,0) .. (\\tikztotarget) \\tikztonodes}] \\node (a) at (0,0) {a}; \\node (b) at (2,1) {b}; \\node (c) at (1,2) {c}; \\draw (a) to node { x} (b) (a) to (c); \\end{ tikzpicture}\n\nHere is another example:",
    "type": "command"
  },
  "\\tikztotarget": {
    "signature": "",
    "default": "",
    "description": "\\begin{ tikzpicture}[ to path={ .. controls +(1,0) and +(1,0) .. (\\tikztotarget) \\tikztonodes}] \\node (a) at (0,0) {a}; \\node (b) at (2,1) {b}; \\node (c) at (1,2) {c}; \\draw (a) to node { x} (b) (a) to (c); \\end{ tikzpicture}\n\nHere is another example:",
    "type": "command"
  },
  "\\tikztonodes": {
    "signature": "",
    "default": "",
    "description": "\\begin{ tikzpicture}[ to path={ .. controls +(1,0) and +(1,0) .. (\\tikztotarget) \\tikztonodes}] \\node (a) at (0,0) {a}; \\node (b) at (2,1) {b}; \\node (c) at (1,2) {c}; \\draw (a) to node { x} (b) (a) to (c); \\end{ tikzpicture}\n\nHere is another example:",
    "type": "command"
  },
  "/tikz/execute at begin to": {
    "signature": "/tikz/execute at begin to=⟨code⟩",
    "default": "no default",
    "description": "The ⟨code⟩ is executed prior to the to. This can be used to draw one or more additional paths or to do additional computations.",
    "type": "key"
  },
  "execute at begin to": {
    "signature": "/tikz/execute at begin to=⟨code⟩",
    "default": "no default",
    "description": "The ⟨code⟩ is executed prior to the to. This can be used to draw one or more additional paths or to do additional computations.",
    "type": "key"
  },
  "/tikz/execute at end to": {
    "signature": "/tikz/execute at end to=⟨code⟩",
    "default": "no default",
    "description": "Works like the previous option, only this code is executed after the to path has been added.",
    "type": "key"
  },
  "execute at end to": {
    "signature": "/tikz/execute at end to=⟨code⟩",
    "default": "no default",
    "description": "Works like the previous option, only this code is executed after the to path has been added.",
    "type": "key"
  },
  "foreach": {
    "signature": "\\path … foreach⟨variables⟩[⟨options⟩] in ⟨list⟩ {⟨path commands⟩} …;",
    "default": "",
    "description": "The foreach operation can be used to repeatedly insert the ⟨path commands⟩ into the current path. Naturally, the ⟨path commands⟩ should internally reference some of the ⟨variables⟩ so that you do not insert exactly the same path repeatedly, but rather variations. For historical reasons, you can also write \\foreach instead of foreach.",
    "type": "key"
  },
  "let": {
    "signature": "\\path … let⟨assignment⟩ ,⟨assignment⟩,⟨assignment⟩… in …;",
    "default": "",
    "description": "When this path operation is encountered, the ⟨assignment⟩s are evaluated, one by one. This will store coordinate and number in special registers (which are local to TikZ, they have nothing to do with TeX registers). Subsequently, one can access the contents of these registers using the macros \\p, \\x, \\y, and \\n.\n\nThe first kind of permissible ⟨assignment⟩s have the following form:",
    "type": "key"
  },
  "\\n": {
    "signature": "\\n{⟨number register⟩}",
    "default": "",
    "description": "When this macro is used on the left-hand side of an =-sign in a let operation, it has no effect and is just there for readability. When the macro is used on the right-hand side of an =-sign or in the body of the let operation, then it expands to the value stored in the ⟨number register⟩. This will either be a dimensionless number like 2.0 or a dimension like 5.6pt.\n\nFor instance, if we say let \\n1={1pt+2pt}, \\n2={1+2} in ..., then inside the ... part the macro \\n1 will expand to 3pt and \\n2 expands to 3.",
    "type": "command"
  },
  "\\p": {
    "signature": "\\p{⟨point register⟩}",
    "default": "",
    "description": "When this macro is used on the left-hand side of an =-sign in a let operation, it has no effect and is just there for readability. When the macro is used on the right-hand side of an =-sign or in the body of the let operation, then it expands to the \\(x\\)-part (measured in TeX points) of the coordinate stored in the ⟨register⟩, followed, by a comma, followed by the \\(y\\)-part.\n\nFor instance, if we say let \\p1=(1pt,1pt+2pt) in ..., then inside the ... part the macro \\p1 will expand to exactly the seven characters “1pt,3pt”. This means that you when you write (\\p1), this expands to (1pt,3pt), which is presumably exactly what you intended.",
    "type": "command"
  },
  "\\x": {
    "signature": "\\x{⟨point register⟩}",
    "default": "",
    "description": "This macro expands just to the \\(x\\)-part of the point register. If we say as above, as we did above, let \\p1=(1pt,1pt+2pt) in ..., then inside the ... part the macro \\x1 expands to 1pt.",
    "type": "command"
  },
  "\\y": {
    "signature": "\\y{⟨point register⟩}",
    "default": "",
    "description": "Works like \\x, only for the \\(y\\)-part.\n\nNote that the above macros are available only inside a let operation.",
    "type": "command"
  },
  " ": {
    "signature": "\\path … :⟨animation attribute⟩={⟨options⟩} …;",
    "default": "",
    "description": "This path operation has the same effect as if you had said:\n\n[animate = { myself:⟨animate attribute⟩={⟨options⟩}} ]",
    "type": "key"
  },
  "\\pgfextra": {
    "signature": "\\pgfextra{⟨code⟩}",
    "default": "",
    "description": "This command may only be used inside a TikZ path. There it is used like a normal path operation. The construction of the path is temporarily suspended and the ⟨code⟩ is executed. Then, the path construction is resumed.",
    "type": "command"
  },
  "/tikz/save path": {
    "signature": "/tikz/save path=⟨macro⟩",
    "default": "no default",
    "description": "Save the current soft path into ⟨macro⟩.",
    "type": "key"
  },
  "save path": {
    "signature": "/tikz/save path=⟨macro⟩",
    "default": "no default",
    "description": "Save the current soft path into ⟨macro⟩.",
    "type": "key"
  },
  "/tikz/use path": {
    "signature": "/tikz/use path=⟨macro⟩",
    "default": "no default",
    "description": "Set the current path to the soft path stored in ⟨macro⟩.",
    "type": "key"
  },
  "use path": {
    "signature": "/tikz/use path=⟨macro⟩",
    "default": "no default",
    "description": "Set the current path to the soft path stored in ⟨macro⟩.",
    "type": "key"
  }
}