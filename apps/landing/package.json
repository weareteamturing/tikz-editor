{
  "name": "@tikz-editor/landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:feature-svgs": "node --import tsx scripts/generate-feature-svgs.mts",
    "capture:screenshots": "node --import tsx scripts/capture-screenshots.mts",
    "dev": "npm run generate:feature-svgs && vite",
    "build": "npm run generate:feature-svgs && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@remixicon/react": "^4.9.0",
    "@tikz-editor/app": "0.1.0",
    "gsap": "^3.13.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
