export const TREE_CHILD_LAYOUT_WRITABLE_KEYS = new Set([
  "level distance",
  "sibling distance"
]);

export const TREE_CHILD_NODE_READONLY_KEYS = new Set<string>([]);

export const TREE_ROOT_LAYOUT_KEYS = new Set([
  "grow",
  "level distance",
  "sibling distance"
]);
